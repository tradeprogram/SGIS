# 분석 에이전트(/api/chat) 용 Gemini 키. policymaps 와 같은 변수명을 쓰므로
# 그쪽에서 쓰던 키를 그대로 재사용할 수 있다.
# 미설정이어도 앱은 동작한다 — 화면 값만 안내하는 폴백으로 떨어진다.
GEMINI_API_KEY=
# GOOGLE_API_KEY 도 대체 키로 인식한다.
# 모델명은 필요 시 여기서 바꾼다.
GEMINI_MODEL=gemini-3.6-flash
